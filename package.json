{
  "name": "oasp-website",
  "version": "1.0.0",
  "description": "The website for the OASP.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "gulp",
    "build": "gulp build",
    "deploy": "gulp deploy"
  },
  "private": true,
  "devDependencies": {
    "browser-sync": "2.26.3",
    "gulp": "3.9.1"
  },
  "dependencies": {
    "gulp-sass": "3.2.1",
    "node-sass": "4.9.4",
    "debug": ">=2.6.9"
  }
}
