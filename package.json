{
  "name": "oasp-website",
  "version": "1.0.0",
  "description": "The website for the OASP.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "gulp",
    "deploy": "gulp deploy"
  },
  "private": true,
  "devDependencies": {
    "browser-sync": "2.18.12",
    "gulp": "3.9.1"
  },
  "dependencies": {
    "gulp-sass": "3.1.0"
  }
}
