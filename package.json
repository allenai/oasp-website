{
  "name": "oasp-website",
  "version": "1.0.0",
  "description": "The website for the OASP.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "gulp",
    "build": "gulp build",
    "deploy": "gulp deploy"
  },
  "private": true,
  "devDependencies": {
    "browser-sync": "^2.26.7",
    "extend": "3.0.2",
    "gulp": "3.9.1",
    "tar": "4.4.2"
  },
  "dependencies": {
    "debug": ">=2.6.9",
    "gulp-sass": "3.2.1",
    "node-sass": "^4.12.0"
  }
}
